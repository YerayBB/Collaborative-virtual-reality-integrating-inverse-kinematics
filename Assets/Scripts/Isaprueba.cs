using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Isaprueba : MonoBehaviour
{
    public RootMotion.Demos.VRIKCalibrationController bbfd;
    // Start is called before the first frame update
    void Start()
    {
        //bbfd.rightHandTracker = gameObject.transform;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
